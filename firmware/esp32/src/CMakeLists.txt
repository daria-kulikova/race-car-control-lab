#This CMakeLists.txt is empty since the root CMakeLists.txt file is now
#/ CMakeLists.txt(and not/ src / CMakeLists.txt).It automatically includes the
#main and components directories.

#cmake_minimum_required(VERSION 3.5)
#
#include($ENV{IDF_PATH} / tools / cmake / project.cmake)
#project(car - firmware)
